// Package inbound is the home for driving adapters — the CLI, MCP server, and
// event handlers that call the application's inbound ports. It is empty until
// those entrypoints are built.
package inbound
